version: 0.2

phases:
  build:
    commands:
      - aws s3 sync . s3://x24sousa.com --delete --exclude ".git/*" --exclude "buildspec.yml"
      - aws cloudfront create-invalidation --distribution-id E1N0CWEX76KIBJ --paths "/*"